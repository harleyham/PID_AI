dataset:
  train_dataset:
    img_dir: "/data/dataset_ia/train/images"
    ann_dir: "/data/dataset_ia/train/masks"
  val_dataset:
    img_dir: "/data/dataset_ia/train/images"
    ann_dir: "/data/dataset_ia/train/masks"

model:
  backbone:
    type: "mit_b0"
  num_classes: 2

train:
  batch_size: 2
  max_iters: 500
  checkpoint_interval: 100
  optim_config:
    optimizer: "AdamW"
    lr: 0.00006

results_dir: "/data/results/p2_experimento_01"